# 采集数据交接格式

给负责爬取的同学：**不用建数据库，给一个 JSONL 文件就行。**

一行一篇文章，六个字段。跑一遍自查命令能过，就可以发过来。

---

## 一、格式

```json
{"source_account":"清华大学学生社会实践","source_url":"https://mp.weixin.qq.com/s/AbCdEf123","title":"2026年寒假社会实践支队招募","publish_date":"2026-08-05","raw_text":"为深入学习……报名截止时间为2026年8月20日24:00……","images":["https://mmbiz.qpic.cn/mmbiz_jpg/xxx/640"]}
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `source_account` | 是 | 公众号名称，原样即可 |
| `source_url` | 是 | 文章永久链接，必须是 `https://mp.weixin.qq.com/s/...` |
| `title` | 是 | 文章标题，原样，不用去掉 `【】` |
| `publish_date` | 是 | 发布日期，`YYYY-MM-DD` |
| `raw_text` | 是 | **正文全文纯文本**，保留换行 |
| `images` | 否 | 正文里的图片 URL 数组，没有就别写这个字段 |

文件用 UTF-8，一行一条，不要外层数组，不要美化缩进（那样会跨行）。

---

## 二、`raw_text` 是重点，其余都好说

**要的是正文全文，不是摘要、不是清洗过的、不是提取过的。**

具体来说：

- 保留换行。「报名截止：8月20日」这种冒号行，换行位置本身就是我们判断字段边界的依据，全挤成一行会丢信息。
- 不用删「点击上方蓝字关注」「往期回顾」这类页尾噪音——我们这边有处理，删错了反而麻烦。
- HTML 标签去掉，纯文本即可。
- 不用做任何字段提取。

**为什么不要你替我们解析成结构化字段**（比如直接给「截止日期 = 2026-08-20」）：

抽取规则写在我们这边的 `domain.py` 里，有测试覆盖，而且刻意保守——拿不准的字段会标成 `uncertain` 交给人工核验，而不是猜一个填上。如果两边各解析一遍，我们收到的就是另一套猜测，而且**看不出哪些是猜的**。

这件事的代价不对称：报名截止日期写错一天，用户就错过报名；参与资格写错，用户白准备一场。所以宁可多留一步人工核验，也不要多一层看不见的猜测。

原文是唯一能回查、能重跑、能定位错误来源的东西。你把原文给全，剩下的归我们。

---

## 三、图片型推送怎么办

不少招募推送正文只有一张长图，文字全在图里。这种情况：

**把 `images` 填上，`raw_text` 里有什么写什么**（哪怕只有标题和几行字）。我们这边有 OCR（`wechat_image_ocr.py`）可以补。

不要因为正文太短就跳过——跳过了我们连它存在都不知道。宁可给一条不完整的，也别漏。

---

## 四、发过来之前自查一遍

```bash
python3 scripts/import_articles.py articles.jsonl --check
```

不写数据库，只报格式问题，逐行给出行号。它会拦下这些：

- 缺字段、字段为空
- `source_url` 不是合法的公众号文章链接
- `publish_date` 格式不对
- **`raw_text` 短于 120 字**——多半是抓到了摘要或验证页，而不是正文
- `raw_text` 里出现「请在微信客户端打开链接」「环境异常」——抓到验证页了
- 某一行 JSON 坏掉（只影响该行，会报出行号）

全绿再发，能省掉一轮来回。

---

## 五、增量交付：随时发，不用等齐

**不要等全部爬完再给。** 爬到一批发一批，我们这边按 `source_url` 去重，重复的自动跳过：

```bash
python3 scripts/import_articles.py 第一批.jsonl
python3 scripts/import_articles.py 第二批.jsonl   # 重复的自动跳过
```

早一天拿到第一批，就早一天知道抽取规则在真实数据上有哪些没考虑到——这个反馈越早越值钱。**先发 20 条让我们试跑，比最后一次性给 500 条稳得多。**

---

## 六、抓哪些账号

已配置的默认账号（`collector_settings.py`）：

- 清华大学社会实践
- 无限之声
- 清华大学学生公益

按实际情况增减即可。时间范围建议覆盖**最近 3 个月**——更早的多半已经过了报名截止，进库也只会被标成 `expired`。

---

## 七、常见问题

**能不能直接给 SQLite 文件？** 可以，但请是「原文表」而不是「解析好的项目表」，含上面六个字段即可，我们再转成 JSONL。不过 JSONL 更方便：能直接看、能 grep、能分批追加、坏了能定位到行。

**正文里有报名表格/二维码怎么办？** 表格转成文本保留行列关系即可；二维码放进 `images`。

**同一篇文章被多个号转发？** 都给，我们这边会按标题查重合并。

**爬到一半凭证失效了？** 把已经拿到的发过来，剩下的下一批补。不用回滚。

---

有拿不准的，直接问，不要猜着改格式——格式对不上我们能一眼看出来，猜着改的内容混进去就很难查了。
