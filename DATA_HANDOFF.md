# 数据上传说明

给负责爬取和维护数据的同学。

**你不需要接入我们的服务器、不需要账号、不需要 SSH、不需要装任何包。**
需要的只有两样东西：一个网址和一把密钥（找宋子睿要）。

爬到一篇，处理成下面的格式，POST 过来，几秒后就能在小搭里被推荐出来。

---

## 一、你要产出的格式

每篇文章一个 JSON 对象，六个字段：

```json
{
  "source_account": "清华大学学生社会实践",
  "source_url": "https://mp.weixin.qq.com/s/AbCdEf123",
  "title": "2026年寒假社会实践支队招募",
  "publish_date": "2026-08-05",
  "raw_text": "为深入学习……\n实践地点：云南省大理州\n报名截止：2026年8月20日24:00……",
  "images": ["https://mmbiz.qpic.cn/mmbiz_jpg/xxx/640"]
}
```

| 字段 | 必填 | 说明 |
|---|---|---|
| `source_account` | 是 | 公众号名称，原样 |
| `source_url` | 是 | 文章永久链接，必须是 `https://mp.weixin.qq.com/s/...` |
| `title` | 是 | 标题原样，`【】` 不用去 |
| `publish_date` | 是 | `YYYY-MM-DD` |
| `raw_text` | 是 | **正文全文纯文本，保留换行** |
| `images` | 否 | 正文图片 URL 数组；没有就不写 |

---

## 二、正文只做一件事：去 HTML 标签

**别做别的。** 具体是：

- **保留换行。** 这是最重要的一条。「报名截止：8月20日」这种行，换行位置本身就是我们判断字段边界的依据。全挤成一行的话，我们会把「实践地点」抽成一段夹着报名截止日期的长句——这个坑我们真的踩过。
- **别删页尾噪音。**「点击上方蓝字关注」「往期回顾」这些我们这边有处理，你删错了反而麻烦。
- **别替我们提取字段。** 不用给我们「截止日期 = 2026-08-20」。

第三条解释一下，因为它反直觉：

抽取规则在我们这边，有测试覆盖，而且刻意保守——拿不准的字段会标成待核验交人工，不会猜一个填上。如果两边各解析一遍，我们收到的是另一套猜测，**而且看不出哪些是猜的**。

这件事的代价不对称：截止日期错一天，用户就错过报名；资格写错，用户白准备一场。所以宁可多一步人工核验，也不要多一层看不见的猜测。

原文是唯一能回查、能重跑、能定位错在哪的东西。你把原文给全，剩下的归我们。

**图片型推送**（正文只有长图、文字都在图里）：把 `images` 填上，`raw_text` 里有什么写什么，哪怕只有标题。我们这边有 OCR 可以补。**别因为正文短就跳过**——跳过了我们连它存在都不知道。

---

## 三、怎么传

把仓库里的 `scripts/push_article.py` 复制到你的爬虫目录（只用标准库，不装包）：

```python
from push_article import push_article

push_article({
    "source_account": "清华大学学生社会实践",
    "source_url": "https://mp.weixin.qq.com/s/xxx",
    "title": "……",
    "publish_date": "2026-08-05",
    "raw_text": "正文全文……",
})
```

跑之前设两个环境变量：

```bash
export XIAODA_INGEST_URL=http://8.217.145.109:8000/api/ingest
export XIAODA_INGEST_KEY=<找宋子睿要>
```

想先拿文件试，把上面的 JSON 一行一个存成 `articles.jsonl`，然后：

```bash
python3 push_article.py articles.jsonl
```

网络抖动、限流、5xx 会自动退避重试三次；密钥错或字段缺会直接报错**不重试**——那种情况重试多少次都一样。

---

## 四、返回值怎么读

| status | 含义 | 要处理吗 |
|---|---|---|
| `imported` | 已生成项目卡 | 不用 |
| `not_opportunity` | 原文存了，但判定不是招募（纪实、回顾、行前预告） | **不用**，这是正常的 |
| `needs_text` | 只收到链接没有正文，存成了待补线索 | 补上正文重推 |

`not_opportunity` 不是错误，别重推，也别改内容再发一遍。如果你确定某条其实在招募而我们判错了，**把标题告诉我们**，是我们的规则漏了。

**重复推送同一条是安全的**——按链接认成同一个项目，只补不覆盖，不会在推荐里重复出现。所以爬虫重跑、断点续跑都不用担心。

---

## 五、发现错了怎么改

主办方延期了、名额调整了、资格放宽了，或者你发现之前那版正文抓漏了——重新推一次，**带上 `correction=True`**：

```python
push_article({...修正后的内容...}, correction=True)
```

用文件的话，那一行里加 `"correction": true`。

为什么需要这个标记：平时的合并是「只补不覆盖」，防止转发版、被风控截断的半截正文把完整版洗掉。但订正后的内容常常**更少**——比如资格从「仅限计算机系」改成「面向全校」——按默认规则会被当成信息量下降挡回去，订正就悄悄失效了。带上标记表示「以我这版为准」。

历史不会丢：每次推送的原文都留档，项目也有版本记录，改错了能翻回去。

---

## 六、发之前自查

如果你是攒成文件传：

```bash
python3 scripts/import_articles.py articles.jsonl --check
```

不写数据库，逐行报问题：缺字段、链接不合法、日期格式不对、正文短于 120 字
（多半抓到了摘要或验证页）、正文里出现「请在微信客户端打开链接」（抓到验证页了）、某行 JSON 坏掉。

直接走 API 的话，服务端会返回同样的检查结果。

---

## 七、几件要注意的事

**密钥**：只能投稿，不能改项目、不能导出项目库，所以万一泄漏后果有限。但还是别提交进仓库、别贴群里。

**先发一小批。** 别等全部爬完再传。**先来 20 条**，我们看抽取结果对不对——真实公众号文本上抽取规则会有哪些没考虑到的地方，只有真数据能暴露。这个反馈越早越值钱。

**字段齐全的会自动进正式推荐。** 报名截止、资格、经费、报名方式、原文链接五项都抽到的项目，不经人工直接发布。所以推之前确认正文是完整的——半截正文抽出来的卡片可能字段齐全但内容是错的。

**抓哪些账号**：清华大学社会实践、无限之声、清华大学学生公益是我们默认配的，按实际情况增减。时间范围建议**最近 3 个月**——更早的多半已经过了报名截止，进库也只会被标成已过期。

---

## 八、常见问题

**能不能给 SQLite 文件？** 可以，但请是「原文表」而不是解析好的项目表，含上面六个字段即可。不过 JSONL 更方便：能直接看、能 grep、能分批追加、坏了能定位到行。

**正文里有报名表格/二维码？** 表格转文本保留行列关系；二维码放进 `images`。

**同一篇被多个号转发？** 都推，我们按标题查重合并。

**爬到一半凭证失效？** 把已经拿到的推过来，剩下的下次补，不用回滚。

---

有拿不准的直接问，别猜着改格式——格式对不上我们能一眼看出来，猜着改的内容混进去很难查。
